# nixpacks.toml
# This forces Railway to install BOTH Node and Python
providers = ["node", "python"]

[phases.setup]
# Explicitly install the binaries so they are available in the shell
nixPkgs = ["nodejs", "python3"]

[start]
# Your specific Clawdbot start command
cmd = "node dist/index.js gateway start"