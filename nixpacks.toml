providers = ["node", "python"]

[phases.setup]
 nixPkgs = ["nodejs", "python3"]

[phases.build]
 # This is the magic line that creates the 'dist' folder
 cmds = ["npm install", "npm run build"]

[start]
 cmd = "node dist/index.js gateway start"